<%= form_for @workout do |f| %>
  <%= f.label :name%>
  <%= f.text_field :name %>
  <br />
  <%= f.label :repetitions%>
  <%= f.number_field :repetitions %>
  <br />
  <%= f.label :image%>
  <%= f.file_field :workout_image %>
  <br />
  <%= f.submit 'Save'%>

<% end %>
